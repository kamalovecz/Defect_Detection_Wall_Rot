# Immutable source YAML archive

The six YAML files in this directory are byte-for-byte copies from
`D:\defect_detection\History_Data\harpnet_selected_artifacts` captured for
provenance. Do not edit them. Runnable configurations live one directory up;
the active B4 canonical source remains
`training_project/models/B4_A-GFPN_RepHFE_target.yaml`.

The external provenance path may not exist on another machine. Normal runtime
and default archive verification use only repository-relative paths; external
source comparison is explicitly opt-in.
