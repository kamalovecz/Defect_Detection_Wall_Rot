# Bundled Ultralytics Runtime

This directory contains the pinned Ultralytics 8.2.50 runtime used by the
HARP-Net training project. Project modules are installed through the generic
extension API; this vendor package does not import `defect_modules`.

The source remains licensed under AGPL-3.0. See `LICENSE` for details.
